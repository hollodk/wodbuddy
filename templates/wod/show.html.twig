{% extends 'base.html.twig' %}

{% block title %}Wod{% endblock %}

{% block body %}
    <h1>Wod</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ wod.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ wod.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ wod.description }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ wod.createdAt ? wod.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ wod.updatedAt ? wod.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('wod_index') }}">back to list</a>

    <a href="{{ path('wod_edit', {'id': wod.id}) }}">edit</a>

    {{ include('wod/_delete_form.html.twig') }}
{% endblock %}
